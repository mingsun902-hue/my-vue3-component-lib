import { default as Button } from '../../button/src';
import { default as Input } from '../../input/src';
import { default as Message } from '../../message/src';
import { Dialog, CreateDialog } from '../../dialog/src';
declare const _default: {
    install(app: any): void;
};
export default _default;
export { Button, Input, Message, Dialog, CreateDialog };
