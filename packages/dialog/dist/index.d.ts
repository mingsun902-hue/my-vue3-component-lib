import { createDialog } from './createDialog';
import { default as Dialog } from './Dialog.vue';
export default Dialog;
declare const CreateDialog: typeof createDialog;
export { Dialog, CreateDialog, createDialog };
